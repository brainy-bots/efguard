/**
 * Stub: PolicyGroup objects no longer exist on-chain.
 * Rules are now set directly per assembly via assembly_binding::set_policy.
 * This file is kept as an empty export to avoid breaking any stale imports
 * until they are all cleaned up.
 */
